# Context directory (HEFHS structure)

## Purpose

This directory stores working context data:

- `active/` - Currently active context
- `history/` - Context history
- `template/` - Context templates

## Usage

```python
from context import load_active_context, save_context
```

*Note: Implement context management based on your needs.*